export declare type Photo = {
  id: number;
  albumId: number;
  title: string;
  url: string;
  thumbnailUrl: string;
};

export declare type NewPhoto = {
  albumId: number;
  title: string;
  url: string;
  thumbnailUrl: string;
};
